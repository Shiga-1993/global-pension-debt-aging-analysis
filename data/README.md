# Data Handling

The analysis script reads public data directly from official or public data APIs and does not store heavy raw source files.

Generated aggregate tables are written under `outputs/tables/`.
